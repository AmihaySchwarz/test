import * as React from 'react';
  
  export default class RemarksPanel extends React.Component<any,any> {

    public render() {
      const { selectedItem } = this.state;

      return (
     <div>
      MOdal form for remarks
      </div>
      );
    }
  }